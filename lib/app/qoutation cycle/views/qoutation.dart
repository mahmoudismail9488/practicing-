import 'package:flutter/material.dart';

class Qoutation extends StatelessWidget {
  const Qoutation({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("qoutation"));
  }
}
